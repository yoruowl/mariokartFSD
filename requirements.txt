# RTX 50-series (Blackwell) needs CUDA 13 wheels from pytorch.org.
# `pip install torch` from PyPI installs the CPU-only build.
--extra-index-url https://download.pytorch.org/whl/cu130
torch==2.14.0+cu130
torchvision==0.29.0+cu130
mss==10.2.0
pygame-ce==2.5.8
PyGetWindow==0.0.9
Pillow==12.3.0
vgamepad==0.1.0
